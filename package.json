{
	"name": "root",
	"private": true,
	"scripts": {
		"bootstrap": "lerna bootstrap --force-local",
		"build": "lerna run build",
		"update": "taze major -w",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"lerna": "^6.0.1",
		"nx": "^15.0.0",
		"prettier": "^2.7.1",
		"taze": "^0.8.3"
	}
}
