{
	"name": "root",
	"private": true,
	"scripts": {
		"bootstrap": "lerna bootstrap --force-local",
		"build": "lerna run build",
		"update": "taze latest -w",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"lerna": "^7.4.2",
		"nx": "^17.1.3",
		"prettier": "^3.1.0",
		"taze": "^0.12.0"
	}
}
